import { TaskCompleteDirective } from './task-complete.directive';

describe('TaskCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
